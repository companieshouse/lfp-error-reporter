# ------------------------------------------------------------------------------
# Environment
# ------------------------------------------------------------------------------

environment = "cidev"
aws_profile = "development-eu-west-2"
cron_schedule = "cron(0/30 * ? * * *)"
application_subnet_pattern_key = "APPLICATION_SUBNET_PATTERN"
open_lambda_environment_variables = {LOGLEVEL="trace"}
