# ------------------------------------------------------------------------------
# Environment
# ------------------------------------------------------------------------------

environment = "ernie"
aws_bucket = "ch-development-terraform-state"
workspace_key_prefix = "lfp-error-reporter"
state_file_name = "lfp-error-reporter.tfstate"
cron_schedule = "cron(0 23 * * ? *)"
aws_region = "eu-west-1"


